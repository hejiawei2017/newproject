import { createAction } from 'redux-actions'

export const serachingAuthority = createAction('SEARCH_AUTHORIT_ING',data=>data)
export const serachAuthoritySuccess = createAction('SEARCH_AUTHORIT_SUCCESS',data=>data)

