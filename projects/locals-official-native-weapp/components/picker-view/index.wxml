<view 
  class="popover"
  style="hidden: {{ !visible }}; {{ delayStyle }}"
>
  <view
    class="mask {{ visible ? 'active' : '' }}"
    catch:touchmove="maskTouchMove"
    catch:tap="_bindcancel"
  />
  <view class="content {{ visible ? 'active' : '' }}">
    <slot></slot>
  </view>
</view>