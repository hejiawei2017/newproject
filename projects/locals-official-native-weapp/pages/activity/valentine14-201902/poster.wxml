<scroll-view class="page-container" scrollY="{{true}}">
  <!-- poster -->
  <view class="poster">
    <poster
      id="poster"
      poster-original-width="{{800}}"
      poster-original-height="{{1109}}"
      watermark-horizontal-padding="{{15}}"
      watermark-vertical-padding="{{15}}"
    />
  </view>
  <!-- banner -->
  <button class="save-button" bindtap="saveToAlbum">保存图片</button>
</scroll-view>
